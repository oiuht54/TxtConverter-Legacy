# TxtConverter

**TxtConverter** — это профессиональная десктопная утилита для быстрой и безопасной подготовки исходного кода проектов к анализу нейросетями (LLM), архивации или отправке в чаты.

Приложение сканирует папку проекта и создает оптимизированный единый текстовый файл, который удобно "скармливать" ChatGPT, Claude или DeepSeek.

![Скриншот приложения](docs/screenshot.png)

---

## 🔥 Ключевые возможности

### 🧠 Оптимизация для LLM (ИИ)
*   **Экономия токенов:** Мы заменили громоздкие разделители на минималистичные заголовки (`--- FILE: Name.ext ---`). Это позволяет вместить больше полезного кода в контекстное окно нейросети.
*   **Умное слияние:** Вы можете выбрать, какие файлы включить в отчет **полностью**, а какие оставить в виде **заглушек**.
    *   *Пример:* Если файл не выбран для слияния, в отчете появится строка: `(Содержимое файла опущено для краткости...)`. Это дает ИИ контекст о существовании файла, не тратя токены на его содержимое.

### ⚡ Эффективность и Удобство
*   **Умная сортировка:** Единый файл теперь автоматически именуется как `_(ИмяПроекта)_Full_Source_code.txt`. Символ `_` в начале гарантирует, что файл всегда будет первым в списке проводника.
*   **Современный UI:** Полностью переработанный темный интерфейс (High Contrast Dark Theme) в стиле современных IDE (VS Code / JetBrains).
*   **Обратная связь:** Встроенный **Progress Bar** и статусная строка позволяют отслеживать процесс обработки больших проектов в реальном времени.

### 🛡️ Безопасность
*   **Non-Destructive:** Приложение **никогда** не меняет исходные файлы. Все результаты сохраняются в отдельную папку `_ConvertedToTxt` внутри вашего проекта.
*   **Игнорирование мусора:** Встроенные пресеты автоматически исключают системные папки (`.git`, `node_modules`, `Library`, `target`, `.godot`), чтобы в отчет попадал только чистый код.

### ⚙️ Гибкая настройка
*   **Пресеты:** Готовые настройки для **Unity**, **Godot**, **Java (Maven/Gradle)**, **Web Frontend**.
*   **Древовидный выбор:** Удобное меню выбора файлов с группировкой по расширениям.
*   **Структура проекта:** Опциональная генерация файла `_FileStructure.md`, который рисует дерево папок вашего проекта для лучшего понимания контекста.

---

## 🚀 Как использовать

1.  Запустите `TxtConverter.exe`.
2.  Нажмите **"Выбрать..."** и укажите корневую папку вашего проекта.
3.  Выберите **Пресет** (например, *Unity Engine* или *Godot Engine*). Приложение автоматически заполнит расширения и игнорируемые папки.
4.  Нажмите **"Пересканировать"**, чтобы увидеть список найденных файлов.
5.  (Опционально) Нажмите **"Выбрать файлы..."**, чтобы отметить галочками только те скрипты, которые нужны вам в полном объеме. Остальные файлы будут добавлены в отчет как заглушки.
6.  Убедитесь, что галочка **"Создавать единый файл (_ProjectName_Full_Source_code.txt)"** включена.
7.  Нажмите большую синюю кнопку **"НАЧАТЬ КОНВЕРТАЦИЮ"**.
8.  По завершении зайдите в появившуюся папку `_ConvertedToTxt` и заберите готовый файл.

---

## 🛠️ Сборка из исходников (для разработчиков)

Проект построен на **Java 21** и **JavaFX 21**. Используется архитектура, разделяющая UI и фоновые задачи (Tasks), для обеспечения плавности интерфейса.

### Требования
*   JDK 21+
*   Apache Maven

### Команды сборки

1.  **Клонирование:**
    ```bash
    git clone https://github.com/YourName/TxtConverter.git
    cd TxtConverter
    ```

2.  **Сборка (создание .exe):**
    ```bash
    mvn clean package
    ```
    Maven автоматически скачает зависимости, соберет "fat-jar" и с помощью `jpackage` создаст нативный исполняемый файл.

3.  **Результат:**
    Готовое приложение будет лежать в: `target/jpackage/TxtConverter/`

---

## 💻 Технологический стек

*   **Язык:** Java 21
*   **UI Framework:** JavaFX 21 (FXML + CSS Styling)
*   **Concurrency:** JavaFX `Task<V>` API (фоновое выполнение операций ввода-вывода)
*   **Build System:** Maven
    *   `maven-shade-plugin`: Сборка всех зависимостей в один JAR.
    *   `jpackage-maven-plugin`: Создание самодостаточного установщика/экзешника (JRE включена внутрь, пользователю не нужно устанавливать Java).

---

*TxtConverter — Делаем общение с ИИ проще.*